APScheduler>=3.10,<4
# Explicit rather than transitive via python-telegram-bot: the API sends its
# Telegram notifications through httpx because the Windows host's OS trust store
# cannot verify api.telegram.org, while httpx's bundled CA set can.
httpx>=0.27,<1
python-dotenv>=1,<2
python-telegram-bot>=21,<23

